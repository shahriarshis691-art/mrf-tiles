import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mrfgalaxytiles.app',
  appName: 'MRF Galaxy Tiles',
  webDir: 'out',
};

export default config;
